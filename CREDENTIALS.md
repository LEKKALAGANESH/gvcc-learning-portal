# Test Credentials — GVCC Learning Portal

Seeded by `npm run db` (see `prisma/seed.ts`).

## Demo account
- **Email:** `demo@gvcc.dev`
- **Password:** `password123`

## Create your own
Use the **Sign up** page (`/signup`) — any email + a password of 8+ characters works.

---
_Local dev only. `AUTH_SECRET` in `.env` is a dev placeholder — change it for any real deployment._
